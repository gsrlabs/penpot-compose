# Penpot Self-Hosted Deployment

## 🎨 О проекте

Penpot — это первый инструмент дизайна с открытым исходным кодом, созданный для совместной работы дизайнеров и разработчиков. Дизайнеры могут создавать потрясающие проекты, интерактивные прототипы и масштабируемые дизайн-системы, в то время как разработчики получают готовый к использованию код, что делает их рабочий процесс простым и быстрым. И всё это без проблем передачи макетов.

**Ключевые особенности:**
- Полностью открытый исходный код
- Совместная работа в реальном времени
- Экспорт кода для разработчиков
- Поддержка стандартных форматов (SVG, CSS и др.)
- Не зависит от операционной системы (работает в браузере)

## 📋 Требования

- Сервер с Linux (Ubuntu/Debian рекомендуется)
- Установленный Docker и Docker Compose
- Caddy или другой обратный прокси
- Открытые порты: 80 и 443
- Доменное имя, указывающее на IP-адрес сервера
- Python 3 (для генерации секретного ключа)

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/gsrlabs/penpot-compose.git penpot
cd penpot
```

### 2. Настройка переменных окружения

Создайте файл `.env`:

```env
cat > .env << 'EOF'
PENPOT_VERSION=latest
PENPOT_PUBLIC_URI=https://your-domain.com
PENPOT_SECRET_KEY=your-secret-key-here
EOF
```

 Для редактирования запустите:
```
nano .env
```

**Настройте значения:**

1. **Замените `your-domain.com`** на ваш реальный домен
2. **Сгенерируйте секретный ключ** и вставьте его вместо `your-secret-key-here`:

```bash
python3 -c "import secrets; print(secrets.token_urlsafe(64))"
```

### 3. Настройка обратного прокси (Caddy)

Добавьте в файл `/etc/caddy/Caddyfile`:

```
your-domain.com {
    reverse_proxy 127.0.0.1:9001
}
```

Перезапустите Caddy:

```bash
sudo systemctl reload caddy
```

### 4. Настройка фаервола

```bash
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

### 5. Запуск Penpot

```bash
docker compose -p penpot up -d
```

## 👥 Создание первого пользователя

### Если регистрация включена:
1. Перейдите по вашему домену в браузере
2. Зарегистрируйте первого пользователя через веб-интерфейс

### Если регистрация отключена (ручное создание):

```bash
docker exec -ti penpot-penpot-backend-1 \
  python3 manage.py create-profile
```

Следуйте инструкциям в терминале для создания администратора.

## 🛠️ Управление сервисом

Скрипт `manage.sh` для управления контейнерами Penpot:

**Использование:**

```bash
# Дайте права на выполнение
chmod +x manage.sh

# Запустить все сервисы
./manage.sh start

# Остановить все сервисы
./manage.sh stop

# Перезапустить сервисы
./manage.sh restart

# Просмотр логов в реальном времени
./manage.sh logs

# Показать статус контейнеров
./manage.sh status

# Обновить Penpot до последней версии
./manage.sh update
```

## 🐳 Архитектура Docker Compose

Penpot состоит из нескольких сервисов:

1. **PostgreSQL** — база данных
2. **Redis** — кэширование и очереди
3. **Penpot Backend** — основной сервер API
4. **Penpot Frontend** — веб-интерфейс
5. **Penpot Exporter** (опционально) — экспорт ресурсов

Все сервисы управляются через единый `docker-compose.yml` файл.

## 🔧 Дополнительная настройка

### Настройка базы данных

По умолчанию используется встроенная PostgreSQL. Для внешней БД добавьте в `.env`:

```env
DATABASE_URL=postgresql://user:password@host:port/dbname
```

### Настройка почты для уведомлений

```env
SMTP_HOST=smtp.your-email.com
SMTP_PORT=587
SMTP_USERNAME=your-email@domain.com
SMTP_PASSWORD=your-password
SMTP_SSL=false
SMTP_TLS=true
SMTP_DEFAULT_FROM=Penpot <noreply@your-domain.com>
```

### Включение/отключение регистрации

```env
PENPOT_ENABLE_REGISTRATION=true  # или false для отключения
```

## 📊 Мониторинг и логи

### Просмотр логов отдельных сервисов:

```bash
# Логи backend
docker logs penpot-penpot-backend-1 -f

# Логи frontend
docker logs penpot-penpot-frontend-1 -f

# Логи базы данных
docker logs penpot-postgres-1 -f
```

### Проверка здоровья:

```bash
# Проверить доступность frontend
curl -f http://localhost:9001 || echo "Frontend не отвечает"

# Проверить доступность backend API
curl -f http://localhost:6060/api/health || echo "Backend не отвечает"
```

## 🔄 Обновление

### Стандартное обновление:

```bash
./manage.sh update
```

### Ручное обновление с резервной копией:

```bash
# 1. Создать резервную копию базы данных
docker exec penpot-postgres-1 pg_dump -U penpot penpot > penpot_backup_$(date +%Y%m%d).sql

# 2. Обновить
./manage.sh update

# 3. При проблемах - восстановить из бекапа
cat penpot_backup_*.sql | docker exec -i penpot-postgres-1 psql -U penpot penpot
```

## 🗑️ Удаление

Чтобы полностью удалить Penpot с сервера:

```bash
# Остановить и удалить контейнеры с данными
docker compose -p penpot down -v

# Удалить директорию проекта
cd ..
rm -rf penpot/
```

## 🔒 Рекомендации по безопасности

1. **Всегда используйте HTTPS** через обратный прокси
2. **Регулярно обновляйте** секретный ключ (`PENPOT_SECRET_KEY`)
3. **Настройте регулярное резервное копирование** базы данных
4. **Ограничьте доступ** к административным функциям
5. **Используйте сильные пароли** для пользователей

## 🆘 Устранение неполадок

### Penpot не запускается
```bash
# Проверьте логи
./manage.sh logs

# Проверьте занят ли порт 9001
netstat -tulpn | grep 9001
```

### Проблемы с подключением к БД
```bash
# Проверьте статус PostgreSQL
docker logs penpot-postgres-1

# Проверьте подключение к БД из контейнера backend
docker exec -ti penpot-penpot-backend-1 \
  python3 -c "import psycopg2; psycopg2.connect('dbname=penpot user=penpot host=postgres')"
```

### Проблемы с обратным прокси
```bash
# Проверьте логи Caddy
sudo journalctl -u caddy -f

# Проверьте доступность напрямую
curl http://localhost:9001
```

## 📚 Полезные ссылки

- [Официальный сайт Penpot](https://penpot.app)
- [GitHub репозиторий](https://github.com/penpot/penpot)
- [Официальная документация](https://help.penpot.app)
- [Сообщество и поддержка](https://community.penpot.app)

## 📄 Лицензия

Penpot распространяется под лицензией MPL-2.0 (Mozilla Public License 2.0).

---

**Примечание**: Этот репозиторий содержит минимальную конфигурацию для запуска Penpot. Для продвинутых сценариев использования обратитесь к [официальной документации](https://help.penpot.app/technical-guide/getting-started/docker/).

**Версия**: `latest` (последняя стабильная версия с Docker Hub)